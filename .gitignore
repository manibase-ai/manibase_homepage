# manibase Website — only `site/` (plus repo docs) is tracked.
# Everything below is local working material, not needed to run the website.

# --- OS / editor junk ---
.DS_Store
**/.DS_Store
Thumbs.db
*.swp

# --- Python-Bytecode (entsteht beim Ausfuehren von scripts/*.py) ---
__pycache__/
*.py[cod]

# --- Local tooling / agent config ---
.agents/
.claude/

# --- Brand kit & design source (single source of truth lives here locally) ---
# Optimized, site-ready copies are committed under site/assets & site/styles.
Claude Design/

# --- Customer-supplied source images (optimized .webp copies live in site/assets/) ---
Bilder/

# --- Positioning / research source material ---
KI Klartag/

# --- Large reference material ---
*.pdf

# --- Rohdateien der gelieferten KI-Bilder (aufbereitete Fassungen liegen in site/assets/) ---
incoming/
!incoming/README.md

# --- Vom Kunden gelieferte Helfer-Portraets im Projektstamm (aufbereitet in site/assets/helfer-*.webp) ---
/Anton_Angebotsassistenz*.png
/Bernd_Baudokuhelfer*.png
/Willy*.png

# --- Lokale Zwischenstaende und Pakete (abgeleitet aus site/, nicht ausgeliefert) ---
versions/
exports/

# --- Log der Headless-Browser-Screenshots ---
debug.log
